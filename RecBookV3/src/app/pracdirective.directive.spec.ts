import { PracdirectiveDirective } from './pracdirective.directive';

describe('PracdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PracdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
